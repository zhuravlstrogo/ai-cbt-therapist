---

## ⚙️ Конфигурация

Проект использует файл `config.py` для конфигурации. Токен Telegram бота хранится в переменной `TELEGRAM_API_KEY`.

### Обновление TELEGRAM_API_KEY в config.py

В файле `config.py` найдите строку:

```python
TELEGRAM_API_KEY = '8402546664:AAHGUNWWz9ZUGBLeqxrWE6jdkVXxtZFczDQ'
```

Замените значение на ваш токен:

```python
TELEGRAM_API_KEY = 'ваш_токен_здесь'
```

### Как получить TELEGRAM_API_KEY:
1. Откройте Telegram
2. Найдите бота `@BotFather`
3. Напишите команду `/newbot`
4. Следуйте инструкциям (введите имя и username бота)
5. Скопируйте полученный токен
6. Замените значение `TELEGRAM_API_KEY` в файле `config.py`

**Пример config.py:**
```python
TELEGRAM_API_KEY = '123456789:ABCDefGhIjKlMnOpQrStUvWxYz1a2b3c4d5e'
```

---

### Обновление OPENROUTER_API_KEY в config.py

В файле `config.py` найдите строку:

```python
OPENROUTER_API_KEY='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'
```

Замените значение на ваш API ключ с OpenRouter:

```python
OPENROUTER_API_KEY='ваш_api_ключ_здесь'
```

### Как получить OPENROUTER_API_KEY:
1. Перейдите на сайт https://openrouter.ai/
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел параметров: https://openrouter.ai/settings/keys
4. Нажмите кнопку "Create Key" или используйте существующий ключ
5. Скопируйте API ключ
6. Замените значение `OPENROUTER_API_KEY` в файле `config.py`

**Пример конфигурации:**
```python
OPENROUTER_API_KEY = 'sk-or-v1-abc123def456ghi789jkl012mno345pqr'
```

⚠️ **ВАЖНО:**
- Никогда не передавайте никому `config.py` с Вашими токенами!
- OpenRouter API требует $ для использования - ознакомьтесь с ценами на их сайте


# 🚀 Установка и запуск проекта AI CBT Therapist

## 📋 Последовательность команд установки (Ubuntu/Debian)

### Шаг 1: Обновление системных пакетов
```bash
sudo apt-get update && sudo apt -y install python3-pip
```
---

### Шаг 2: Установка виртуального окружения Python
```bash
sudo apt install python3.12-venv
```
**Примечание:** Система может спросить `Do you want to continue? [Y/n]` - введите `Y` и нажмите Enter.

---

### Шаг 3: Установка системных зависимостей для аудио
```bash
sudo apt-get install ffmpeg portaudio19-dev
```
**Примечание:** Система может спросить `Do you want to continue? [Y/n]` - введите `Y` и нажмите Enter.

---

### Шаг 4: Создание виртуального окружения Python
```bash
python3 -m venv myenv
```
**Примечание:** Эта команда может выполняться некоторое время (обычно 10-30 секунд). Ждите, пока не появится новая строка в терминале.

---

### Шаг 5: Запуск терминального мультиплексера (опционально)
```bash
tmux
```

---

### Шаг 6: Активация виртуального окружения
```bash
source myenv/bin/activate
```

---

### Шаг 7: Переход в папку проекта
```bash
cd ai-cbt-therapist
```

**Примечание:** В этой папке находятся файлы `main.py`, `requirements.txt` и другие файлы проекта.

---

### Шаг 8: Установка Python зависимостей проекта
```bash
cat requirements.txt | xargs -n 1 pip install
```

**Примечание:** Эта команда может выполняться долго (2-5 минут) - подождите завершения. В консоли будут видны логи установки каждого пакета.

---

### Шаг 9: Запуск приложения
```bash
python main.py
```
**Для остановки бота:** Нажмите `Ctrl+C` в терминале.


---

## 🔧 Решение проблем

### Ошибка: "Permission denied"
Добавьте `sudo` перед командой установки или измените права доступа:
```bash
chmod +x myenv/bin/activate
```

### Ошибка: "python3: command not found"
Установите Python:
```bash
sudo apt-get install python3
```

### Ошибка: "No module named 'telebot'"
Убедитесь, что:
1. Виртуальное окружение активировано (видите `(myenv)` в начале строки)
2. Вы в корневой папке проекта (`cd ai-cbt-therapist`)
3. Зависимости установлены (`pip install -r requirements.txt`)

### Ошибка при распознавании речи
Убедитесь, что установлены FFmpeg и PortAudio:
```bash
ffmpeg -version
which ffmpeg
```

Если FFmpeg не установлен:
```bash
sudo apt-get install ffmpeg
```

### Ошибка: "Bot token is invalid"
Проверьте:
1. Файл `config.py` существует в корневой папке проекта
2. Значение `TELEGRAM_API_KEY` скопировано полностью и правильно
3. Токен активен в Telegram (попробуйте создать новый через @BotFather)

---

## 💡 Дополнительно


### Повторный запуск бота 
Просто выполните:
```bash
source myenv/bin/activate
cd ai-cbt-therapist
python main.py
```
(Шаги 1-3 нужны только один раз при первой установке)

### Обновление зависимостей
Если в `requirements.txt` появились новые пакеты:
```bash
pip install -r requirements.txt
```
(или `cat requirements.txt | xargs -n 1 pip install` - для установки по одному)
